import 'package:deev_test/model/model.dart';
import 'package:flutter/material.dart';

List<ContainerCard> listContainers = [
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
  ContainerCard(color: Colors.grey),
];
